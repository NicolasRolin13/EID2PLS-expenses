machine:
  python:
    version: 3.6.2

dependencies:
  pre:
    - pip install requirements.txt

test:
  pre:
    - ./pre-test-script.sh


deployment:
  production:
    branch: master
    commands:
      - ./deploy-to-production.sh