machine:
  python:
    version: 3.6.2

dependencies:
  pre:
    - pip install requirements.txt

test:
  pre:
    - ./manage.py collectstatic --no-input


deployment:
  production:
    branch: master
    commands:
      - ./deploy-to-production.sh