machine:
  python:
    version: 3.6.2

dependencies:
  pre:
    - pip install -r requirements.txt

test:
  override:
    - ./flake8.sh
    - python setup.py test


deployment:
  production:
    branch: master
    commands:
      - ./deploy-to-production.sh